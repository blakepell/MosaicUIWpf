<UserControl
    x:Class="Mosaic.UI.Wpf.Controls.SideMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:Mosaic.UI.Wpf.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:Mosaic.UI.Wpf.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="SideMenuInstance"
    d:DesignHeight="450"
    d:DesignWidth="300"
    Loaded="SideMenuInstance_Loaded"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Margin="0,0,0,5"
            SnapsToDevicePixels="True"
            Visibility="{Binding SearchBoxVisibility, ElementName=SideMenuInstance}">
            <local:SearchBox
                x:Name="SearchBox"
                FontSize="14"
                Watermark="Search List">
                <i:Interaction.Behaviors>
                    <behaviors:ItemsControlFilterBehavior TargetItemsControl="{Binding ElementName=MenuOptions}" />
                </i:Interaction.Behaviors>
            </local:SearchBox>
        </Border>

        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="MenuOptions" ItemsSource="{Binding MenuItems, ElementName=SideMenuInstance}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:SideMenuItem}">
                        <local:SideMenuItemControl
                            Margin="0,1,0,0"
                            Cursor="Hand"
                            MenuItem="{Binding}"
                            SideMenu="{Binding ElementName=SideMenuInstance}"
                            Visibility="{Binding Visibility}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
