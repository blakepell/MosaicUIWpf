<UserControl x:Class="Mosaic.UI.Wpf.Controls.PieChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls"
             mc:Ignorable="d"
             SizeChanged="UserControl_SizeChanged"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Canvas Grid.Row="1"
                Grid.Column="1"
                x:Name="MainCanvas"
                Margin="0,0,0,8" />

        <ScrollViewer Grid.Row="2"
                      Grid.Column="1"
                      x:Name="Legend"
                      SizeChanged="LegendColumn_OnSizeChanged"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Categories, RelativeSource={RelativeSource AncestorType={x:Type controls:PieChart}}, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type controls:PieCategory}">
                        <StackPanel Orientation="Horizontal"
                                    Margin="8">
                            <Rectangle Width="24"
                                       Height="24"
                                       Fill="{Binding EffectiveBrush}"
                                       Margin="0,0,8,0"
                                       VerticalAlignment="Center"
                                       Stroke="Gray"/>

                            <TextBlock Text="{Binding Name}"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="-"
                                       Margin="8,0"
                                       VerticalAlignment="Center" />
                            <!--  Percentage is calculated by the chart from Value and the total of every value.  -->
                            <TextBlock Text="{Binding Percentage, StringFormat={}{0:0.#}%}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
