<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Mosaic.UI.Wpf.Controls"
    xmlns:themes="clr-namespace:Mosaic.UI.Wpf.Themes">

    <Style TargetType="{x:Type local:ContentPanel}">
        <Setter Property="Background" Value="{DynamicResource {x:Static themes:MosaicTheme.MessageBoxHeaderBackgroundBrush}}" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlTextForegroundBrush}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlBorderBrush}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HeaderBackground" Value="{DynamicResource {x:Static themes:MosaicTheme.MessageBoxHeaderBackgroundBrush}}" />
        <Setter Property="HeaderForeground" Value="{DynamicResource {x:Static themes:MosaicTheme.WindowForegroundBrush}}" />
        <Setter Property="FooterBackground" Value="{DynamicResource {x:Static themes:MosaicTheme.MessageBoxFooterBackgroundBrush}}" />
        <Setter Property="FooterForeground" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlTextForegroundBrush}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ContentPanel}">
                    <local:ClipBorder
                        Background="{TemplateBinding FooterBackground}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        Opacity="{TemplateBinding Opacity}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Border
                                x:Name="PART_Header"
                                Grid.Row="0"
                                Padding="10"
                                Background="{TemplateBinding HeaderBackground}"
                                TextElement.Foreground="{TemplateBinding HeaderForeground}"
                                Visibility="{TemplateBinding HeaderVisibility}">
                                <DockPanel LastChildFill="True">
                                    <TextBlock
                                        FontFamily="{TemplateBinding FontFamily}"
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Foreground="{TemplateBinding HeaderForeground}"
                                        Text="{TemplateBinding Title}" />
                                    <ContentPresenter Content="{TemplateBinding HeaderContent}" TextElement.Foreground="{TemplateBinding HeaderForeground}" />
                                </DockPanel>
                            </Border>

                            <Border
                                x:Name="PART_HeaderSeparator"
                                Grid.Row="1"
                                Height="1"
                                Background="{DynamicResource {x:Static themes:MosaicTheme.ControlSeparatorBrush}}"
                                Opacity="0.45"
                                Visibility="{TemplateBinding SeparatorVisibility}" />

                            <Border
                                Grid.Row="2"
                                Padding="10"
                                Background="{TemplateBinding Background}"
                                TextElement.Foreground="{TemplateBinding Foreground}">
                                <ContentPresenter x:Name="PART_Content" TextElement.Foreground="{TemplateBinding Foreground}" />
                            </Border>

                            <Border
                                Grid.Row="3"
                                Padding="10"
                                Background="{TemplateBinding FooterBackground}"
                                BorderBrush="{DynamicResource {x:Static themes:MosaicTheme.ControlSeparatorBrush}}"
                                BorderThickness="0,1,0,0"
                                TextElement.Foreground="{TemplateBinding FooterForeground}">
                                <ContentPresenter Content="{TemplateBinding FooterContent}" TextElement.Foreground="{TemplateBinding FooterForeground}" />
                            </Border>
                        </Grid>
                    </local:ClipBorder>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HeaderVisibility" Value="Collapsed">
                            <Setter TargetName="PART_Header" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_HeaderSeparator" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="HeaderVisibility" Value="Hidden">
                            <Setter TargetName="PART_Header" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_HeaderSeparator" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
