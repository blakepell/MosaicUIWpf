<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls"
    xmlns:themes="clr-namespace:Mosaic.UI.Wpf.Themes">
    <Style TargetType="{x:Type controls:EditableTextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlTextForegroundBrush}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:EditableTextBlock}">
                    <Grid
                        x:Name="PART_GridContainer"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Background="{TemplateBinding Background}">
                        <TextBlock
                            x:Name="PART_TbDisplayText"
                            Grid.Row="0"
                            Padding="{TemplateBinding Padding}"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            Cursor="Hand"
                            Foreground="{TemplateBinding Foreground}"
                            Text="{Binding Mode=TwoWay, Path=Text, RelativeSource={RelativeSource AncestorType={x:Type controls:EditableTextBlock}}}"
                            ToolTip="{TemplateBinding ToolTip}"
                            Visibility="Visible" />
                        <TextBox
                            x:Name="PART_TbEditText"
                            Grid.Row="0"
                            Padding="{TemplateBinding TextBoxPadding}"
                            VerticalAlignment="Center"
                            Background="{DynamicResource {x:Static themes:MosaicTheme.ControlTextBackgroundBrush}}"
                            Foreground="{DynamicResource {x:Static themes:MosaicTheme.ControlTextForegroundBrush}}"
                            Text="{Binding Mode=TwoWay, Path=Text, RelativeSource={RelativeSource AncestorType={x:Type controls:EditableTextBlock}}}"
                            Visibility="Collapsed" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>