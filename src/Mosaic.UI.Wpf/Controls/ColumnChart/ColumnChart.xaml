<UserControl
    x:Class="Mosaic.UI.Wpf.Controls.ColumnChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    SizeChanged="UserControl_SizeChanged"
    mc:Ignorable="d">
    <!--
        The theme brushes (Background, Foreground, ColumnBrush and StrokeBrush) are applied as dynamic resource
        references in the constructor rather than here, because a UserControl's root element can only set
        properties declared by UserControl itself.
    -->
    <Grid>
        <Canvas
            x:Name="MainCanvas"
            HorizontalAlignment="Center"
            VerticalAlignment="Center" />
    </Grid>
</UserControl>
