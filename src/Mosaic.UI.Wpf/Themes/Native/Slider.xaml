<!-- https://github.com/WPFDevelopersOrg/WPFDevelopers/blob/master/src/WPFDevelopers.Shared/Styles/Styles.Slider.xaml -->
<ResourceDictionary
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls"
         xmlns:common="clr-namespace:Mosaic.UI.Wpf.Common"
         xmlns:convert="clr-namespace:Mosaic.UI.Wpf.Converters"
         xmlns:themes="clr-namespace:Mosaic.UI.Wpf.Themes"
         xmlns:system="clr-namespace:System;assembly=System.Runtime">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Mosaic.UI.Wpf;component/Controls/Animations.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <convert:CornerRadiusToSurroundConverter x:Key="CornerRadiusToSurroundConverter" />
    <system:Double x:Key="RepeatButtonSize">6</system:Double>
    <Style
        x:Key="SliderThumbStyle"
        TargetType="{x:Type Thumb}">
        <Setter Property="Width" Value="12" />
        <Setter Property="Height" Value="12" />
        <Setter Property="Background" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlBackgroundBrush}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:MosaicTheme.AccentBrush}}" />
        <Setter Property="BorderThickness" Value="1.5" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="ThumbMouseOver">
                            <DoubleAnimation
                                Storyboard.TargetName="PART_Border"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                From="1.0"
                                To="1.15"
                                Duration="00:00:0.1" />
                            <DoubleAnimation
                                Storyboard.TargetName="PART_Border"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                From="1.0"
                                To="1.15"
                                Duration="00:00:0.1" />
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Border
                        Name="PART_Border"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6"
                        RenderTransformOrigin=".5,.5"
                        SnapsToDevicePixels="True">
                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1" />
                        </Border.RenderTransform>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="BeginStoryboard" Storyboard="{StaticResource ThumbMouseOver}" />
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="BeginStoryboard" />
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="SliderRepeatButtonStyle"
        TargetType="{x:Type controls:SliderRepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SliderRepeatButton}">
                    <Border
                        x:Name="PART_Border"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Background="{TemplateBinding Background}" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="RadiusOrientation" Value="Right">
                            <Setter TargetName="PART_Border" Property="CornerRadius" Value="{Binding Path=(common:ElementHelper.CornerRadius), RelativeSource={RelativeSource AncestorType=Slider}, Converter={StaticResource CornerRadiusToSurroundConverter}, ConverterParameter=Right}" />
                        </Trigger>
                        <Trigger Property="RadiusOrientation" Value="Left">
                            <Setter TargetName="PART_Border" Property="CornerRadius" Value="{Binding Path=(common:ElementHelper.CornerRadius), RelativeSource={RelativeSource AncestorType=Slider}, Converter={StaticResource CornerRadiusToSurroundConverter}, ConverterParameter=Left}" />
                        </Trigger>
                        <Trigger Property="RadiusOrientation" Value="Down">
                            <Setter TargetName="PART_Border" Property="CornerRadius" Value="{Binding Path=(common:ElementHelper.CornerRadius), RelativeSource={RelativeSource AncestorType=Slider}, Converter={StaticResource CornerRadiusToSurroundConverter}, ConverterParameter=Down}" />
                        </Trigger>
                        <Trigger Property="RadiusOrientation" Value="Up">
                            <Setter TargetName="PART_Border" Property="CornerRadius" Value="{Binding Path=(common:ElementHelper.CornerRadius), RelativeSource={RelativeSource AncestorType=Slider}, Converter={StaticResource CornerRadiusToSurroundConverter}, ConverterParameter=Up}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <ControlTemplate x:Key="SliderHorizontal" TargetType="{x:Type Slider}">
        <Border
            Padding="{TemplateBinding Padding}"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            ClipToBounds="True"
            SnapsToDevicePixels="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" MinHeight="16" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TickBar
                    x:Name="TopTick"
                    Grid.Row="0"
                    Height="4"
                    Margin="0,0,0,2"
                    Fill="{TemplateBinding Foreground}"
                    Placement="Top"
                    Visibility="Collapsed" />
                <TickBar
                    x:Name="BottomTick"
                    Grid.Row="2"
                    Height="4"
                    Margin="0,2,0,0"
                    Fill="{TemplateBinding Foreground}"
                    Placement="Bottom"
                    Visibility="Collapsed" />

                <Track x:Name="PART_Track" Grid.Row="1" Margin="6,1,6,1">
                    <Track.DecreaseRepeatButton>
                        <controls:SliderRepeatButton
                            Height="{StaticResource RepeatButtonSize}"
                            Margin="0,2,0,0"
                            Background="{DynamicResource {x:Static themes:MosaicTheme.ControlSeparatorBrush}}"
                            BorderBrush="{DynamicResource Button.Static.Border}"
                            BorderThickness="1"
                            Command="{x:Static Slider.DecreaseLarge}"
                            RadiusOrientation="Right"
                            Style="{DynamicResource SliderRepeatButtonStyle}" />
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                        <controls:SliderRepeatButton
                            Height="{StaticResource RepeatButtonSize}"
                            Margin="0,2,0,0"
                            Background="{DynamicResource {x:Static themes:MosaicTheme.ControlSeparatorBrush}}"
                            BorderBrush="{DynamicResource Button.Static.Border}"
                            BorderThickness="1"
                            Command="{x:Static Slider.IncreaseLarge}"
                            RadiusOrientation="Left"
                            Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource SliderThumbStyle}" />
                    </Track.Thumb>
                </Track>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <ControlTemplate x:Key="SliderVertical" TargetType="{x:Type Slider}">
        <Border
            Padding="{TemplateBinding Padding}"
            HorizontalAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            ClipToBounds="True"
            SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" MinWidth="16" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TickBar
                    x:Name="TopTick"
                    Grid.Column="0"
                    Width="4"
                    Margin="0,0,2,0"
                    Fill="{DynamicResource {x:Static themes:MosaicTheme.ControlTextSecondaryForegroundBrush}}"
                    Placement="Left"
                    Visibility="Collapsed" />
                <TickBar
                    x:Name="BottomTick"
                    Grid.Column="2"
                    Width="4"
                    Margin="2,0,0,0"
                    Fill="{DynamicResource {x:Static themes:MosaicTheme.ControlTextSecondaryForegroundBrush}}"
                    Placement="Right"
                    Visibility="Collapsed" />
                <Track x:Name="PART_Track" Grid.Column="1" Margin="1,0,1,0">
                    <Track.DecreaseRepeatButton>
                        <controls:SliderRepeatButton
                            Width="{StaticResource RepeatButtonSize}"
                            Background="{DynamicResource {x:Static themes:MosaicTheme.ControlSeparatorBrush}}"
                            BorderBrush="{DynamicResource Button.Static.Border}"
                            BorderThickness="1"
                            Command="{x:Static Slider.DecreaseLarge}"
                            RadiusOrientation="Up"
                            Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                        <controls:SliderRepeatButton
                            Width="{StaticResource RepeatButtonSize}"
                            Background="{DynamicResource {x:Static themes:MosaicTheme.ControlSeparatorBrush}}"
                            BorderBrush="{DynamicResource Button.Static.Border}"
                            BorderThickness="1"
                            Command="{x:Static Slider.IncreaseLarge}"
                            RadiusOrientation="Down"
                            Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource SliderThumbStyle}" />
                    </Track.Thumb>
                </Track>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style
        x:Key="DefaultSlider"
        TargetType="{x:Type Slider}">
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
        <Setter Property="IsMoveToPointEnabled" Value="True" />
        <Setter Property="Background" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlBackgroundBrush}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlBorderBrush}}" />
        <Setter Property="SelectionEnd" Value="{Binding RelativeSource={RelativeSource Self}, Path=Value}" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlTextForegroundBrush}}" />
        <Setter Property="Template" Value="{StaticResource SliderHorizontal}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Template" Value="{StaticResource SliderVertical}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style BasedOn="{StaticResource DefaultSlider}" TargetType="{x:Type Slider}" />
</ResourceDictionary>