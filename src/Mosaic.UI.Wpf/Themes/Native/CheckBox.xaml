<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options">

    <!--<Style x:Key="OptionMarkEmptyFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle
                        Width="15"
                        Height="15"
                        Margin="-2,-3,-2,-1"
                        SnapsToDevicePixels="True"
                        Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                        StrokeDashArray="1 1"
                        StrokeDashOffset="1.5"
                        StrokeThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="OptionMarkFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle
                        Margin="15,1,1,2"
                        SnapsToDevicePixels="True"
                        Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                        StrokeDashArray="1 1"
                        StrokeDashOffset="1.5"
                        StrokeThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Geometry x:Key="Checkbox.CheckMark">
        F1 M0.86,5.88 2.699,4.182 5.699,7.432 4.78,8.28 3.804,7.499 8.804,1.249 10.756,2.811 4.848,10.196 3.861,9.128 0.861,5.878 z
    </Geometry>

    <Geometry x:Key="Checkbox.IndeterminateMark">
        F1 M0,0 5,0 5,5 0,5 z
    </Geometry>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource OptionMarkFocusVisual}" />
        <Setter Property="Background" Value="{DynamicResource CheckBox.Background.Normal}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CheckBox.Border.Normal}" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="3,2,2,3" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid Background="Transparent" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border
                            x:Name="Border"
                            Margin="0,0,2,0"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid Width="11" Height="11">
                                <Path
                                    x:Name="Mark"
                                    Fill="{DynamicResource CheckBox.Mark.Normal}"
                                    Stretch="None" />
                            </Grid>
                        </Border>
                        <ContentPresenter
                            x:Name="Content"
                            Grid.Column="1"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CheckBox.Border.Hot}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CheckBox.Background.Hot}" />
                            <Setter TargetName="Mark" Property="Fill" Value="{DynamicResource CheckBox.Mark.Hot}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CheckBox.Border.Pressed}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CheckBox.Background.Pressed}" />
                            <Setter TargetName="Mark" Property="Fill" Value="{DynamicResource CheckBox.Mark.Pressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CheckBox.Border.Disabled}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CheckBox.Background.Disabled}" />
                            <Setter TargetName="Mark" Property="Fill" Value="{DynamicResource CheckBox.Mark.Disabled}" />
                        </Trigger>
                        <Trigger Property="HasContent" Value="False">
                            <Setter Property="FocusVisualStyle" Value="{DynamicResource OptionMarkEmptyFocusVisual}" />
                            <Setter Property="Padding" Value="0" />
                        </Trigger>
                        <Trigger Property="TextOptions.TextFormattingMode" Value="Display">
                            <Setter TargetName="Border" Property="Margin" Value="0,-1,0,0" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Mark" Property="Data" Value="{DynamicResource Checkbox.CheckMark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="Mark" Property="Data" Value="{DynamicResource Checkbox.IndeterminateMark}" />
                            <Setter TargetName="Mark" Property="Margin" Value="3" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
            </Trigger>
        </Style.Triggers>
    </Style>-->
</ResourceDictionary>