<UserControl
    x:Class="MosaicWpfDemo.Controls.Header"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mosaic="http://schemas.apexgate.net/wpf/mosaic-ui"
    xmlns:themes="clr-namespace:Mosaic.UI.Wpf.Themes;assembly=Mosaic.UI.Wpf"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Image
                    Width="48"
                    Height="48"
                    HorizontalAlignment="Left"
                    Source="{Binding Path=ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <TextBlock
                    Margin="4,7,7,4"
                    FontSize="22"
                    SnapsToDevicePixels="True"
                    Text="{Binding Path=Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <mosaic:Shield
                Grid.Column="1"
                Margin="0,5"
                VerticalAlignment="Center"
                Label="Mosaic.UI.Wpf"
                ValueBackgroundBrush="#14663A">
                <mosaic:VersionTextBlock AssemblyName="Mosaic.UI.Wpf" AssemblySource="ByName" />
            </mosaic:Shield>
        </Grid>

        <Separator Margin="8,4,0,4" Background="{DynamicResource {x:Static themes:MosaicTheme.ControlSeparatorBrush}}" />

    </StackPanel>
</UserControl>
