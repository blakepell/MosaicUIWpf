<UserControl
    x:Class="MosaicWpfDemo.Views.Examples.SyntaxEditorExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls;assembly=Mosaic.UI.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MosaicWpfDemo.Views.Examples"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Margin="0,0,0,8"
            Text="A themed code editor with JSON syntax highlighting. Toggle the app theme (top-right) to see the editor recolor and swap its highlighting. Right-click for Cut/Copy/Paste/Select All plus Format, Minify, and Validate JSON."
            TextWrapping="Wrap" />

        <!--  FollowGlobalTheme is disabled here to demonstrate explicit theme wiring from the code-behind.  -->
        <controls:SyntaxEditor
            x:Name="Editor"
            Grid.Row="1"
            FollowGlobalTheme="False"
            Language="Json" />
    </Grid>
</UserControl>
