<UserControl
    x:Class="MosaicWpfDemo.Views.Examples.MarkdownViewerExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls;assembly=Mosaic.UI.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MosaicWpfDemo.Views.Examples"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:themes="clr-namespace:Mosaic.UI.Wpf.Themes;assembly=Mosaic.UI.Wpf"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Margin="0,0,0,8"
            Text="A WPF-native Markdown viewer. Markdown is rendered into a FlowDocument hosted in a read-only RichTextBox, so the formatted text can be selected and copied as rich text instead of raw Markdown syntax. Select any text below and press Ctrl+C to copy it."
            TextWrapping="Wrap" />

        <Border
            Grid.Row="1"
            Padding="12"
            Background="{DynamicResource {x:Static themes:MosaicTheme.ControlBackgroundBrush}}"
            BorderBrush="{DynamicResource {x:Static themes:MosaicTheme.ControlBorderBrush}}"
            BorderThickness="1"
            CornerRadius="4">
            <controls:MarkdownViewer x:Name="Viewer" FontSize="14" />
        </Border>
    </Grid>
</UserControl>
