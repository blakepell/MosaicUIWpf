<UserControl
    x:Class="MosaicWpfDemo.Views.Examples.FavoriteCheckBoxExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls;assembly=Mosaic.UI.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MosaicWpfDemo.Views.Examples"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock
            Margin="0,0,0,12"
            FontWeight="SemiBold"
            Text="Both controls share the same bound favorite state." />

        <StackPanel Margin="0,0,0,10" Orientation="Horizontal">
            <controls:FavoriteCheckBox
                Margin="0,0,8,0"
                IsChecked="{Binding IsFavorite, Mode=TwoWay}"
                Symbol="&#x2605;"
                SymbolSize="24" />
            <TextBlock VerticalAlignment="Center" Text="Star favorite" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <controls:FavoriteCheckBox
                Margin="0,0,8,0"
                IsChecked="{Binding IsFavorite, Mode=TwoWay}"
                HoverBrush="Pink"
                CheckedBrush="Red"
                Symbol="&#x2665;"
                SymbolSize="24" />
            <TextBlock VerticalAlignment="Center" Text="Heart favorite" />
        </StackPanel>
    </StackPanel>
</UserControl>
