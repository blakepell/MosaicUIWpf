<UserControl
    x:Class="MosaicWpfDemo.Views.Examples.ProgressRingExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls;assembly=Mosaic.UI.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MosaicWpfDemo.Views.Examples"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:themes="clr-namespace:Mosaic.UI.Wpf.Themes;assembly=Mosaic.UI.Wpf"
    d:DesignHeight="600"
    d:DesignWidth="800"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="ExampleCaptionStyle" TargetType="TextBlock">
            <Setter Property="Grid.Row" Value="1" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="0,10,0,0" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:MosaicTheme.ControlTextSecondaryForegroundBrush}}" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock
                Margin="0,0,0,15"
                FontSize="14"
                Foreground="{DynamicResource {x:Static themes:MosaicTheme.ControlTextSecondaryForegroundBrush}}"
                Text="ProgressRing displays an indeterminate, animated spinner to indicate that a long running operation is in progress. Set IsActive to start or stop the animation; the ring shows six trailing dots at larger sizes and five at smaller sizes. By default it is painted with the current Mosaic accent brush."
                TextWrapping="Wrap" />

            <!--  Start / Stop toggle  -->
            <CheckBox
                x:Name="ActiveToggle"
                Margin="0,0,0,20"
                Content="IsActive"
                IsChecked="True" />

            <WrapPanel ItemHeight="190" ItemWidth="180">
                <!--  Small (5 dots)  -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <controls:ProgressRing
                        Width="30"
                        Height="30"
                        IsActive="{Binding IsChecked, ElementName=ActiveToggle}" />
                    <TextBlock Style="{StaticResource ExampleCaptionStyle}" Text="Small (30px)" />
                </Grid>

                <!--  Medium  -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <controls:ProgressRing
                        Width="50"
                        Height="50"
                        IsActive="{Binding IsChecked, ElementName=ActiveToggle}" />
                    <TextBlock Style="{StaticResource ExampleCaptionStyle}" Text="Medium (50px)" />
                </Grid>

                <!--  Large (6 dots)  -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <controls:ProgressRing
                        Width="80"
                        Height="80"
                        IsActive="{Binding IsChecked, ElementName=ActiveToggle}" />
                    <TextBlock Style="{StaticResource ExampleCaptionStyle}" Text="Large (80px)" />
                </Grid>

                <!--  Extra Large  -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <controls:ProgressRing
                        Width="120"
                        Height="120"
                        IsActive="{Binding IsChecked, ElementName=ActiveToggle}" />
                    <TextBlock Style="{StaticResource ExampleCaptionStyle}" Text="Extra Large (120px)" />
                </Grid>

                <!--  Custom Foreground  -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <controls:ProgressRing
                        Width="80"
                        Height="80"
                        Foreground="{DynamicResource {x:Static themes:MosaicTheme.SuccessBrush}}"
                        IsActive="{Binding IsChecked, ElementName=ActiveToggle}" />
                    <TextBlock Style="{StaticResource ExampleCaptionStyle}" Text="Custom Foreground" />
                </Grid>

                <!--  In Context  -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <controls:ProgressRing
                            Width="24"
                            Height="24"
                            VerticalAlignment="Center"
                            IsActive="{Binding IsChecked, ElementName=ActiveToggle}" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource {x:Static themes:MosaicTheme.ControlForegroundBrush}}"
                            Text="Loading…" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource ExampleCaptionStyle}" Text="Inline With Text" />
                </Grid>
            </WrapPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
