<UserControl
    x:Class="MosaicWpfDemo.Views.Examples.MarkdownEditorExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Mosaic.UI.Wpf.Controls;assembly=Mosaic.UI.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MosaicWpfDemo.Views.Examples"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Margin="0,0,0,8"
            Text="A markdown editor built on the Mosaic SyntaxEditor (AvalonEdit). Use the toolbar to format text, continue lists with Enter, indent with Tab/Shift+Tab, and right-click for 'Copy as HTML' and 'Paste Image as Base64'. The window title below the toolbar tracks unsaved changes with an asterisk."
            TextWrapping="Wrap" />

        <controls:MarkdownEditor
            x:Name="Editor"
            Grid.Row="1"
            StatusBarVisibility="Visible"
            StorageFolder="C:\Temp" />
    </Grid>
</UserControl>
