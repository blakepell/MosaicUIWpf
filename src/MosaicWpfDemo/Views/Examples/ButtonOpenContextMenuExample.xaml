<UserControl
    x:Class="MosaicWpfDemo.Views.Examples.ButtonOpenContextMenuExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:Mosaic.UI.Wpf.Behaviors;assembly=Mosaic.UI.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="12">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock
                Margin="0,0,0,8"
                Text="Click the button to open its text menu."
                TextAlignment="Center" />

            <Button
                Padding="12,8"
                HorizontalAlignment="Center"
                Content="Open menu">
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Copy" />
                        <MenuItem Header="Paste" />
                        <MenuItem Header="Select All" />
                    </ContextMenu>
                </Button.ContextMenu>

                <i:Interaction.Behaviors>
                    <behaviors:OpenContextMenuBehavior Placement="Right" />
                </i:Interaction.Behaviors>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
