# FrameworkElementZoomFontSizeOnMouseWheelBehavior

A behavior that enables zooming the font size of a FrameworkElement using the mouse wheel while the Ctrl key is held.

![FrameworkElementZoomFontSizeOnMouseWheelBehavior](./images/FrameworkElementZoomFontSizeOnMouseWheelBehavior.png)

